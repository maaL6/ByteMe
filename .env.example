# Development examples only. Copy to .env for Docker Compose.
DB_NAME=starter
DB_USERNAME=starter
DB_PASSWORD=local-development-only
DB_ROOT_PASSWORD=local-development-root-only
DB_PORT=3306
SERVER_PORT=8080
# Optional for a local JVM; Compose uses its internal mysql hostname.
# DB_URL=jdbc:mysql://localhost:3306/starter
