# Development examples only. Copy to .env for Docker Compose.
DB_NAME=starter
DB_USERNAME=starter
DB_PASSWORD=local-development-only
DB_PORT=5432
SERVER_PORT=8080
# Optional for a local JVM; Compose uses its internal postgres hostname.
# DB_URL=jdbc:postgresql://localhost:5432/starter
